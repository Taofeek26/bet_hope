# Bet_Hope Requirements
# Django + Python AI Football Prediction Platform
#
# Production runtime deps only — see requirements-dev.txt for
# testing/linting/docs tooling, never installed here.
#
# Used by: the Django web app container, whether running on EC2 (Docker
# Compose) or as a Lambda container image (backend/Dockerfile.lambda-web)
# behind API Gateway REST — see infrastructure/template.yaml.

# =============================================================================
# CORE DJANGO
# =============================================================================
Django>=5.0,<5.2
djangorestframework>=3.14.0
django-cors-headers>=4.3.0
django-filter>=23.5,<25.0
django-extensions>=3.2.3
drf-spectacular>=0.27.0  # OpenAPI schema generation

# =============================================================================
# DATABASE
# =============================================================================
psycopg2-binary>=2.9.9  # PostgreSQL adapter
django-environ>=0.11.2   # Environment variable management
dj-database-url>=2.1.0   # Database URL parsing

# =============================================================================
# AUTHENTICATION
# =============================================================================
djangorestframework-simplejwt>=5.3.1
django-allauth>=0.59.0

# =============================================================================
# ASYNC TASKS
# =============================================================================
celery>=5.3.4
django-celery-beat>=2.5.0
django-celery-results>=2.5.1
redis>=5.0.1

# =============================================================================
# MACHINE LEARNING
# =============================================================================
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.3.2
xgboost>=2.0.2
joblib>=1.3.2
scipy>=1.11.4

# Hyperparameter tuning
optuna>=3.4.0

# =============================================================================
# AI / NLP
# =============================================================================
# sentence-transformers pulls in torch as a hard dependency. Without this,
# pip installs PyPI's default Linux torch wheel, which bundles the full
# CUDA stack (nvidia-cublas/cudnn/nvshmem/etc, several GB) even though
# nothing in this deployment (EC2, Lambda) has a GPU — embeddings run on
# CPU only. This pulls the CPU-only build instead, which has no CUDA deps
# at all. Must come before sentence-transformers so pip resolves torch
# from here first. See: https://pytorch.org/get-started/locally/
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
sentence-transformers>=2.2.2
transformers>=4.35.0
huggingface-hub>=0.19.0
tiktoken>=0.5.2

# AI Provider SDKs
openai>=1.3.0
anthropic>=0.18.0
google-generativeai>=0.3.0

# =============================================================================
# VECTOR DATABASE
# =============================================================================
pgvector>=0.2.4  # PostgreSQL vector extension

# Alternative: Qdrant (uncomment if using)
# qdrant-client>=1.6.0

# =============================================================================
# HTTP & API CLIENTS
# =============================================================================
httpx>=0.25.0
requests>=2.31.0
aiohttp>=3.9.0

# =============================================================================
# DATA VALIDATION
# =============================================================================
pydantic>=2.5.0
pydantic-settings>=2.1.0

# =============================================================================
# UTILITIES
# =============================================================================
python-dateutil>=2.8.2
pytz>=2023.3
python-slugify>=8.0.1
Pillow>=10.1.0  # Image processing
feedparser>=6.0.10  # RSS/Atom feed parsing

# =============================================================================
# CACHING
# =============================================================================
django-redis>=5.4.0

# =============================================================================
# LOGGING & MONITORING
# =============================================================================
sentry-sdk>=1.34.0
structlog>=23.2.0
python-json-logger>=2.0.7

# =============================================================================
# PRODUCTION
# =============================================================================
gunicorn>=21.2.0
uvicorn>=0.24.0
whitenoise>=6.6.0  # Static file serving

# WSGI-to-Lambda adapter — translates API Gateway REST proxy events into
# WSGI calls against the Django app. Only exercised by
# backend/lambda_web_handler.py, harmless/unused on EC2.
apig-wsgi>=2.18.0

# Dev/test/docs tooling (pytest, mypy, black, mkdocs, etc) lives in
# requirements-dev.txt — never installed in the production/Lambda image.
