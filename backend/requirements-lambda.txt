# Bet_Hope Lambda Requirements — minimal, CPU-only, no dev tools
# ~200MB installed vs 2GB for full requirements.txt

# Core Django
Django>=5.0,<5.2
djangorestframework>=3.14.0
django-cors-headers>=4.3.0
django-filter>=23.5,<25.0
django-environ>=0.11.2
dj-database-url>=2.1.0

# Database
psycopg2-binary>=2.9.9

# Auth
djangorestframework-simplejwt>=5.3.1

# Async (Celery not needed on Lambda, but keep for compat)
celery>=5.3.4
redis>=5.0.1

# ML — CPU only
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.3.2
xgboost>=2.0.2
joblib>=1.3.2
scipy>=1.11.4

# AWS SDK
boto3>=1.34.0

# HTTP
requests>=2.31.0
httpx>=0.25.0

# Utilities
python-dateutil>=2.8.2
pytz>=2023.3

# Production
gunicorn>=21.2.0
whitenoise>=6.6.0